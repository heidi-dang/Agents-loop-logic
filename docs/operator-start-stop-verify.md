# Operator Guide: Start, Stop, Verify
(Pending integration of CLI hooks)
1. Edit `suite.json` using config template generator.
2. `heidi model serve` to start.
3. Check `heidi model status` for PID verification.

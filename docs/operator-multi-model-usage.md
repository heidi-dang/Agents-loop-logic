# Operator Guide: Multi-Model Routing
(Pending deep integration in open-webui)
Route completions by explicitly setting the `model` parameter in the API call to match the `models[].id` in your `suite.json`.
